#script for constraint satisfaction checks
feasible <- function(x, problem_parameters){
  #check imposed constraint(s)
  if (<check if infeasible>){
    return(FALSE)
  }
  #if not triggered, then return TRUE
  return(TRUE)
}
